контекст {
    пер всего: int
}

состояние Счёт {
    пер n: int = 0
    на прибавить -> Self { n += 1; всего += 1 }
    на показать -> Показ
}

состояние Показ {
    всегда -> Счёт
    вход { печать("n="); печать("?\n") }
}
