state Start {
    enter { print("Entered Start!") }
    on go -> Middle
}

state Middle {
    enter { print("Entered Middle!") }
    on go -> End
}

state End {
    enter { print("Entered End!") }
    on go -> Start
}

entry main {
    print("B+ state machine started")
}