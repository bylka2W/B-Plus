#parser

state HttpParser {
    on h -> ExpectHeader
    on g -> ExpectGet
}

state ExpectHeader {
    @hot(0.9)
    on colon -> ReadingValue
    on newline -> HttpParser
}

state ReadingValue {
    @hot(0.95)
    on newline -> HttpParser
}
