-- B+ v2.1.3VS — LLVM IR codegen test
-- kernel → .ll (LLVM IR) → .obj via llc

#lang b+ v2.0
#mode turbo
#target auto

@region(frame)
kernel identity(src: Image[H,W]) -> Image[H,W]
    body:
        src |> relu |> clamp(0.0, 1.0)

entry main()
    run my_pipeline
    ExitCode::Ok
