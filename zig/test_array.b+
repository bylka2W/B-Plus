state Work {
    var buf: int = 0
    var i: int = 3
    enter {
        buf = 0
        buf = 42
        i = 2
        var val = buf
        if (val == 42) { } else { }
    }
    always -> Work
}

entry main {
    always { }
}
