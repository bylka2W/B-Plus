state app {
    pid: int64
}

entry main {
    let kernel32 = LoadLibraryA("kernel32.dll");
    let fn_ptr = GetProcAddress(kernel32, "GetCurrentProcessId");
    let pid = call_indirect(fn_ptr);
    pid = pid;
}
