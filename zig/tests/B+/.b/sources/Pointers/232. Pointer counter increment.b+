struct Counter
{
    value: i64
}

fn increment(counter)
{
    p = &counter.value

    *p += 1
}

fn main()
{
    counter = Counter{}
    counter.value = 0

    for i = 0;
        i < 100;
        i = i + 1
    {
        increment(counter)
    }

    print(counter.value)
}