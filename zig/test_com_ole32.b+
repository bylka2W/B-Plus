state app {
    hr: int64
}

entry main {
    let ole32 = LoadLibraryA("ole32.dll");
    let CoInitializeEx = GetProcAddress(ole32, "CoInitializeEx");
    let hr = call_indirect(CoInitializeEx, 0, 0);
    hr = hr;
}
