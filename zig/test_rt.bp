extern fn print_i64(x: i64);
extern fn bplus_malloc(size: i64) i64;
extern fn bplus_free(ptr: i64);
extern fn bplus_exit(code: i64);

fn main() {
    var p = bplus_malloc(16);
    print_i64(p);
    bplus_free(p);
    bplus_exit(0);
}
