enum Direction { Up, Down, Left, Right }

state Player {
    var dir: Direction
    on move -> Player { dir = Right }
    enter { print("Player ready"); }
}

entry main()
    print("Enum test passed")
    return 0